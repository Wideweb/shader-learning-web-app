import { Routes } from "@angular/router";
import { UserRankedListComponent } from "./components/user-ranked-list/user-ranked-list.component";

export const routes: Routes = [
  { path: '', component: UserRankedListComponent },
];
